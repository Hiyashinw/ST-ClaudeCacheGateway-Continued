# 更新日志，AI在会话开始，读取文件后，自动将## 最近一次 移动到## 历史记录中

## 最近一次：

### 外观与诊断可视化

- 请求日志的“Prefix 锁定 / 缓存锚点”栏目改为中文键值分行显示，列表隐藏 Prefix 哈希；块哈希变更使用黄色标签，部分缓存锚点失效使用红色标签。
- 请求列表和 Usage 详情改为双列对齐布局，同时为不同类型的数据标记颜色，缓存命中显示在缓存创建之前，命中率按红 / 黄 / 蓝 / 绿四档着色。
- 高级配置新增全局 Usage 外观面板，可分别设置 8 类数字的文字色与背景色、即时预览并恢复默认。
- 详情页面会在候选点原位置显示未发送的缓存候选，使用仅含“未使用缓存点”的紧凑灰色单行框；Prefix Lock 前缀视图保持不变。

## 历史记录#1

### 改进：
- 尾锚点现在在它成为待淘汰锚点前不会被释放，并在诊断中显示保护 / 待淘汰状态。避免因为最末锚点移动而导致的缓存命中率下降。
- 详情中的每个提示词块将生成 SHA-256 哈希，并与同渠道、同协议、同模型的上一条请求对比；10 位短哈希移至消息标题栏，变化或新增块的哈希加红，完整值保留在诊断 JSON。
- 请求列表和 Usage 详情统一显示“输入 / 输出”“缓存创建 / 缓存命中”“缓存命中率”三行五项统计，并移除旧徽标。
- 新增 `default-gateway-settings.json`，缺少全局字段时作为回退配置。

### 新选项，具体可移步至使用文档
- 新增“末锚点自动使用 5 分钟”，即字面意思，把最末锚点的 1h TTL 转为 5m。
- 新增“忽略末尾锚点”，具体有2种模式：数字模式表示删除最后 x 个锚点；评估模式表示从 x=0 开始，在 3 次同范围成功请求后按块哈希的最早变化边界一次性计算结果。0–5 自动填入，6 以上保留真实值并等待用户明确确认或重新评估。

### 修复
- 修复消息卡折叠后 block 内缓存断点随正文一起消失的问题；有断点的卡片收起时仅隐藏内容块，缓存断点及其精确路径继续显示，无断点卡片仍完全折叠。


## 历史记录#2

### 系统身份消息处理

- 新增“系统身份消息处理”功能，主要有3个选项：“默认”“关闭Anthropic优化”和“统一将系统身份消息放至最顶部”。
- “默认”模式下，OpenAI-compatible 请求转发到 OpenAI-compatible 上游时保持 `system`、`user`、`assistant` 的身份与原始顺序；转换为 Anthropic native 时进行优化：保留开头的 `system`，将中途的 `system` 原位转换为 `user`，并合并连续的同角色发言。
- “关闭Anthropic优化”模式下，OpenAI-compatible 转发保持原样；转换为 Anthropic native 时将所有 `system` 消息移至顶层以避免出现错误。
- “统一将系统身份消息放至最顶部”模式下，无论上游协议为何，都会将所有 `system` 消息稳定地移动到最前方。该模式通常有利于形成稳定的缓存前缀，但可能改变中途系统消息原有的上下文语义，更容易掉思维链。

### 修复

- 修复无法获取 Anthropic 原生思维链的问题。
- 详情页将“提示词总量”改为“输入提示词”，只显示响应 usage 得到的准确 token 数，不再显示字符数或“约 xxx token”。

## 历史记录#3

- 优化滚动锚点：缓存断点首次确定后保持固定，轮换时仅替换最老的非固定锚点，提高缓存命中率。
- 清理请求日志中多余的 - 占位符。
- 改进提示词统计，同时显示字符数与 Token 估计。